# disable default security config from security boot
# don't autoconfigure spring security because we want control that by profiles
# to make testing easier and choose to enable security or not.
# and because we don't want the auto configured security http basic filter.
# (basic http by default)
security.basic.enabled=false
# disable security for actuator endpoints
management.security.enabled=false

